(ns ihub.insights-api.core-test
  (:require
   [ihub.insights-api.test-utils :as utils]
   [clojure.test :refer [deftest is testing]]))


(testing "Example test"
  (deftest example-test
    (is (= 1 1))))